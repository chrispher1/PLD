/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StatusValidatorDirective } from './statusValidator.directive';

describe('Directive: StatusValidator', () => {
  it('should create an instance', () => {
    const directive = new StatusValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
