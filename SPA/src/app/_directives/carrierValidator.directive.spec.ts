/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CarrierValidatorDirective } from './carrierValidator.directive';

describe('Directive: CarrierValidator', () => {
  it('should create an instance', () => {
    const directive = new CarrierValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
