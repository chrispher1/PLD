/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ProductValidatorDirective } from './productValidator.directive';

describe('Directive: ProductValidator', () => {
  it('should create an instance', () => {
    const directive = new ProductValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
