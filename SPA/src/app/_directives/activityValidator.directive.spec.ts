/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ActivityValidatorDirective } from './activityValidator.directive';

describe('Directive: ActivityValidator', () => {
  it('should create an instance', () => {
    const directive = new ActivityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
