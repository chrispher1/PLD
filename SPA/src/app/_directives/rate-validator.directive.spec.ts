import { RateValidatorDirective } from './rate-validator.directive';

describe('RateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
