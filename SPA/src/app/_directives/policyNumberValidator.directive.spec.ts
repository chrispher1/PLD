/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PolicyNumberValidatorDirective } from './policyNumberValidator.directive';

describe('Directive: PolicyNumberValidator', () => {
  it('should create an instance', () => {
    const directive = new PolicyNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
